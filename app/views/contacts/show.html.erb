<div>
  <h1><%= @contact.name %></h1>

  <div>
    <ul>
      <li>Spécialités : <%= @contact.speciality %></li>
      <li>Tel : <%= @contact.phone_number %></li>
      <li><p> Adresse :
            <%= @contact.street %>,
            <%= @contact.zipcode %>
            <%= @contact.city %>,
            <%= @contact.country %></p>
      </li>
    </ul>
    <p><% if @user.admin == true || @contact.user == true %>
      <%= link_to 'Modifier le contact', edit_contact_path(@contact) %>
      <% end %></p>

    <p><% if @user.admin == true %>
      <%= link_to 'Supprimer le contact', contact_path(@contact),
                            method: :delete,
                            data: { confirm: "Are you sure?" } %>
      <% end %></p>
  </div>

  <div>
    <h4>Itinéraire</h4>
    <div>
      <%# Insérer MapBox ici %>
    </div>
  </div>
</div>


  <div class="card">

      <div class="img">
        <h2>Clinique Machin</h2>
      </div>

      <div class="content">
        <ul>
          <!-- Caractéristiques vétérinaires/associations -->
          <li><span class="list-label">Référent </span>: Docteur Fly</li>
          <li><span class="list-label">Adresse </span>: street zipcode city country</li>
          <li><span class="list-label">Spécialité </span>: Mammifère</li>
          <li><span class="list-label">Numéro de téléphone </span>: 0491456789</li>
        </ul>
      </div>
    </div>
