<h2><%= contact.name %></h2>
<p><%= contact.address %></p>
<p><strong> Accueil :</strong></p>
<% contact.contact_categories.each do |contact_category| %>
<p><%= contact_category.category.name %></p>
<% end %>

<%= link_to 'Voir la fiche', contact_path(contact) %>
